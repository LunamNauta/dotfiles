const userOptions = {
    time: {
        interval: 1000,
        format: '%H:%M:%S'
    },
    date: {
        interval: 1000,
        format: '%A, %e/%m'
    },
    cpu:{
        interval: 1000,
        format: " {cpu}%"
    },
    memory: {
        interval: 1000,
        format: " {mem}%"
    }
}

export{
    userOptions
}
