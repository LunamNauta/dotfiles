import type { package_list } from "../../../src/types";

let packages: package_list = {
    pacman: [
        "obs-studio"
    ],
    aur: [
        "wlrobs"
    ],
    flatpak: null
};

export { packages };